#include "../include/testlib.h"

int add(int a, int b) {
    printf("Adding %d and %d\n", a, b);
    return a + b;
}

int sub(int a, int b) {
    printf("Subtracting %d and %d\n", a, b);
    return a - b;
}
